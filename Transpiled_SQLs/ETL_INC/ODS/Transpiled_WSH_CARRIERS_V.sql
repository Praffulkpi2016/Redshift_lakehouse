TRUNCATE table silver_bec_ods.WSH_CARRIERS_V;
INSERT INTO silver_bec_ods.WSH_CARRIERS_V (
  carrier_id,
  scac_code,
  freight_code,
  manifesting_enabled_flag,
  currency_code,
  carrier_name,
  active,
  party_usg_assignment_id,
  attribute_category,
  attribute1,
  attribute2,
  attribute3,
  attribute4,
  attribute5,
  attribute6,
  attribute7,
  attribute8,
  attribute9,
  attribute10,
  attribute11,
  attribute12,
  attribute13,
  attribute14,
  attribute15,
  creation_date,
  created_by,
  last_update_date,
  last_updated_by,
  last_update_login,
  max_num_stops_permitted,
  max_total_distance,
  max_total_time,
  allow_intersperse_load,
  max_layover_time,
  min_layover_time,
  max_total_distance_in_24hr,
  max_driving_time_in_24hr,
  max_duty_time_in_24hr,
  allow_continuous_move,
  max_cm_distance,
  max_cm_time,
  max_cm_dh_distance,
  max_cm_dh_time,
  max_size_width,
  max_size_height,
  max_size_length,
  min_size_width,
  min_size_height,
  min_size_length,
  time_uom,
  dimension_uom,
  distance_uom,
  max_out_of_route,
  cm_free_dh_mileage,
  min_cm_distance,
  cm_first_load_discount,
  min_cm_time,
  unit_rate_basis,
  weight_uom,
  volume_uom,
  generic_flag,
  freight_bill_auto_approval,
  freight_audit_line_level,
  supplier_id,
  supplier_site_id,
  cm_rate_variant,
  distance_calculation_method,
  origin_dstn_surcharge_level,
  dim_dimensional_factor,
  dim_weight_uom,
  dim_volume_uom,
  dim_dimension_uom,
  dim_min_pack_vol,
  kca_operation,
  is_deleted_flg,
  kca_seq_id,
  kca_seq_date
)
SELECT
  carrier_id,
  scac_code,
  freight_code,
  manifesting_enabled_flag,
  currency_code,
  carrier_name,
  active,
  party_usg_assignment_id,
  attribute_category,
  attribute1,
  attribute2,
  attribute3,
  attribute4,
  attribute5,
  attribute6,
  attribute7,
  attribute8,
  attribute9,
  attribute10,
  attribute11,
  attribute12,
  attribute13,
  attribute14,
  attribute15,
  creation_date,
  created_by,
  last_update_date,
  last_updated_by,
  last_update_login,
  max_num_stops_permitted,
  max_total_distance,
  max_total_time,
  allow_intersperse_load,
  max_layover_time,
  min_layover_time,
  max_total_distance_in_24hr,
  max_driving_time_in_24hr,
  max_duty_time_in_24hr,
  allow_continuous_move,
  max_cm_distance,
  max_cm_time,
  max_cm_dh_distance,
  max_cm_dh_time,
  max_size_width,
  max_size_height,
  max_size_length,
  min_size_width,
  min_size_height,
  min_size_length,
  time_uom,
  dimension_uom,
  distance_uom,
  max_out_of_route,
  cm_free_dh_mileage,
  min_cm_distance,
  cm_first_load_discount,
  min_cm_time,
  unit_rate_basis,
  weight_uom,
  volume_uom,
  generic_flag,
  freight_bill_auto_approval,
  freight_audit_line_level,
  supplier_id,
  supplier_site_id,
  cm_rate_variant,
  distance_calculation_method,
  origin_dstn_surcharge_level,
  dim_dimensional_factor,
  dim_weight_uom,
  dim_volume_uom,
  dim_dimension_uom,
  dim_min_pack_vol,
  kca_operation,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  KCA_SEQ_DATE
FROM bronze_bec_ods_stg.WSH_CARRIERS_V;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'wsh_carriers_v';