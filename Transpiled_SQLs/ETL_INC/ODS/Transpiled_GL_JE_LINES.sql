/* Delete Records */
DELETE FROM silver_bec_ods.gl_je_lines
WHERE
  (JE_HEADER_ID, JE_LINE_NUM) IN (
    SELECT
      stg.JE_HEADER_ID,
      stg.JE_LINE_NUM
    FROM silver_bec_ods.gl_je_lines AS ods, bronze_bec_ods_stg.gl_je_lines AS stg
    WHERE
      ods.JE_HEADER_ID = stg.JE_HEADER_ID
      AND ods.JE_LINE_NUM = stg.JE_LINE_NUM
      AND stg.kca_operation IN ('INSERT', 'UPDATE')
  );
/* Insert records */
INSERT INTO silver_bec_ods.gl_je_lines (
  JE_HEADER_ID,
  JE_LINE_NUM,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  LEDGER_ID,
  CODE_COMBINATION_ID,
  PERIOD_NAME,
  EFFECTIVE_DATE,
  STATUS,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  ENTERED_DR,
  ENTERED_CR,
  ACCOUNTED_DR,
  ACCOUNTED_CR,
  DESCRIPTION,
  REFERENCE_1,
  REFERENCE_2,
  REFERENCE_3,
  REFERENCE_4,
  REFERENCE_5,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  ATTRIBUTE16,
  ATTRIBUTE17,
  ATTRIBUTE18,
  ATTRIBUTE19,
  ATTRIBUTE20,
  CONTEXT,
  CONTEXT2,
  INVOICE_DATE,
  TAX_CODE,
  INVOICE_IDENTIFIER,
  INVOICE_AMOUNT,
  NO1,
  STAT_AMOUNT,
  IGNORE_RATE_FLAG,
  CONTEXT3,
  USSGL_TRANSACTION_CODE,
  SUBLEDGER_DOC_SEQUENCE_ID,
  CONTEXT4,
  SUBLEDGER_DOC_SEQUENCE_VALUE,
  REFERENCE_6,
  REFERENCE_7,
  REFERENCE_8,
  REFERENCE_9,
  REFERENCE_10,
  JGZZ_RECON_STATUS_11I,
  JGZZ_RECON_DATE_11I,
  JGZZ_RECON_ID_11I,
  JGZZ_RECON_REF_11I,
  JGZZ_RECON_CONTEXT_11I,
  GLOBAL_ATTRIBUTE_CATEGORY,
  GLOBAL_ATTRIBUTE1,
  GLOBAL_ATTRIBUTE2,
  GLOBAL_ATTRIBUTE3,
  GLOBAL_ATTRIBUTE4,
  GLOBAL_ATTRIBUTE5,
  GLOBAL_ATTRIBUTE6,
  GLOBAL_ATTRIBUTE7,
  GLOBAL_ATTRIBUTE8,
  GLOBAL_ATTRIBUTE9,
  GLOBAL_ATTRIBUTE10,
  TAXABLE_LINE_FLAG,
  TAX_TYPE_CODE,
  TAX_CODE_ID,
  TAX_ROUNDING_RULE_CODE,
  AMOUNT_INCLUDES_TAX_FLAG,
  TAX_DOCUMENT_IDENTIFIER,
  TAX_DOCUMENT_DATE,
  TAX_CUSTOMER_NAME,
  TAX_CUSTOMER_REFERENCE,
  TAX_REGISTRATION_NUMBER,
  TAX_LINE_FLAG,
  TAX_GROUP_ID,
  LINE_TYPE_CODE,
  GL_SL_LINK_ID,
  GL_SL_LINK_TABLE,
  CO_THIRD_PARTY,
  CO_PROCESSED_FLAG,
  KCA_OPERATION,
  IS_DELETED_FLG,
  KCA_SEQ_ID,
  kca_seq_date
)
(
  SELECT
    JE_HEADER_ID,
    JE_LINE_NUM,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    LEDGER_ID,
    CODE_COMBINATION_ID,
    PERIOD_NAME,
    EFFECTIVE_DATE,
    STATUS,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    ENTERED_DR,
    ENTERED_CR,
    ACCOUNTED_DR,
    ACCOUNTED_CR,
    DESCRIPTION,
    REFERENCE_1,
    REFERENCE_2,
    REFERENCE_3,
    REFERENCE_4,
    REFERENCE_5,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    ATTRIBUTE16,
    ATTRIBUTE17,
    ATTRIBUTE18,
    ATTRIBUTE19,
    ATTRIBUTE20,
    CONTEXT,
    CONTEXT2,
    INVOICE_DATE,
    TAX_CODE,
    INVOICE_IDENTIFIER,
    INVOICE_AMOUNT,
    NO1,
    STAT_AMOUNT,
    IGNORE_RATE_FLAG,
    CONTEXT3,
    USSGL_TRANSACTION_CODE,
    SUBLEDGER_DOC_SEQUENCE_ID,
    CONTEXT4,
    SUBLEDGER_DOC_SEQUENCE_VALUE,
    REFERENCE_6,
    REFERENCE_7,
    REFERENCE_8,
    REFERENCE_9,
    REFERENCE_10,
    JGZZ_RECON_STATUS_11I,
    JGZZ_RECON_DATE_11I,
    JGZZ_RECON_ID_11I,
    JGZZ_RECON_REF_11I,
    JGZZ_RECON_CONTEXT_11I,
    GLOBAL_ATTRIBUTE_CATEGORY,
    GLOBAL_ATTRIBUTE1,
    GLOBAL_ATTRIBUTE2,
    GLOBAL_ATTRIBUTE3,
    GLOBAL_ATTRIBUTE4,
    GLOBAL_ATTRIBUTE5,
    GLOBAL_ATTRIBUTE6,
    GLOBAL_ATTRIBUTE7,
    GLOBAL_ATTRIBUTE8,
    GLOBAL_ATTRIBUTE9,
    GLOBAL_ATTRIBUTE10,
    TAXABLE_LINE_FLAG,
    TAX_TYPE_CODE,
    TAX_CODE_ID,
    TAX_ROUNDING_RULE_CODE,
    AMOUNT_INCLUDES_TAX_FLAG,
    TAX_DOCUMENT_IDENTIFIER,
    TAX_DOCUMENT_DATE,
    TAX_CUSTOMER_NAME,
    TAX_CUSTOMER_REFERENCE,
    TAX_REGISTRATION_NUMBER,
    TAX_LINE_FLAG,
    TAX_GROUP_ID,
    LINE_TYPE_CODE,
    GL_SL_LINK_ID,
    GL_SL_LINK_TABLE,
    CO_THIRD_PARTY,
    CO_PROCESSED_FLAG,
    KCA_OPERATION,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.gl_je_lines
  WHERE
    kca_operation IN ('INSERT', 'UPDATE')
    AND (JE_HEADER_ID, JE_LINE_NUM, kca_seq_id) IN (
      SELECT
        JE_HEADER_ID,
        JE_LINE_NUM,
        MAX(kca_seq_id)
      FROM bronze_bec_ods_stg.gl_je_lines
      WHERE
        kca_operation IN ('INSERT', 'UPDATE')
      GROUP BY
        JE_HEADER_ID,
        JE_LINE_NUM
    )
);
/* Soft Delete */
UPDATE silver_bec_ods.gl_je_lines SET IS_DELETED_FLG = 'N';
UPDATE silver_bec_ods.gl_je_lines SET IS_DELETED_FLG = 'Y'
WHERE
  (JE_HEADER_ID, JE_LINE_NUM) IN (
    SELECT
      JE_HEADER_ID,
      JE_LINE_NUM
    FROM bec_raw_dl_ext.gl_je_lines
    WHERE
      (JE_HEADER_ID, JE_LINE_NUM, KCA_SEQ_ID) IN (
        SELECT
          JE_HEADER_ID,
          JE_LINE_NUM,
          MAX(KCA_SEQ_ID) AS KCA_SEQ_ID
        FROM bec_raw_dl_ext.gl_je_lines
        GROUP BY
          JE_HEADER_ID,
          JE_LINE_NUM
      )
      AND kca_operation = 'DELETE'
  );
UPDATE bec_etl_ctrl.batch_ods_info SET last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'gl_je_lines';