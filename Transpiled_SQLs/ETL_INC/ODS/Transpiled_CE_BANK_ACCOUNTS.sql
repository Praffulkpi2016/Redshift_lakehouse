/* Delete Records */
DELETE FROM silver_bec_ods.ce_bank_accounts
WHERE
  BANK_ACCOUNT_ID IN (
    SELECT
      stg.BANK_ACCOUNT_ID
    FROM silver_bec_ods.ce_bank_accounts AS ods, bronze_bec_ods_stg.ce_bank_accounts AS stg
    WHERE
      ods.BANK_ACCOUNT_ID = stg.BANK_ACCOUNT_ID
      AND stg.kca_operation IN ('INSERT', 'UPDATE')
  );
/* Insert records */
INSERT INTO silver_bec_ods.ce_bank_accounts (
  BANK_ACCOUNT_ID,
  BANK_ACCOUNT_NAME,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_LOGIN,
  CREATION_DATE,
  CREATED_BY,
  BANK_ACCOUNT_NUM,
  BANK_BRANCH_ID,
  BANK_ID,
  CURRENCY_CODE,
  DESCRIPTION,
  ATTRIBUTE_CATEGORY,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  REQUEST_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  CHECK_DIGITS,
  BANK_ACCOUNT_NAME_ALT,
  ACCOUNT_HOLDER_ID,
  EFT_REQUESTER_IDENTIFIER,
  SECONDARY_ACCOUNT_REFERENCE,
  ACCOUNT_SUFFIX,
  DESCRIPTION_CODE1,
  DESCRIPTION_CODE2,
  IBAN_NUMBER,
  SHORT_ACCOUNT_NAME,
  ACCOUNT_OWNER_PARTY_ID,
  ACCOUNT_OWNER_ORG_ID,
  ACCOUNT_CLASSIFICATION,
  AP_USE_ALLOWED_FLAG,
  AR_USE_ALLOWED_FLAG,
  XTR_USE_ALLOWED_FLAG,
  PAY_USE_ALLOWED_FLAG,
  MULTI_CURRENCY_ALLOWED_FLAG,
  PAYMENT_MULTI_CURRENCY_FLAG,
  RECEIPT_MULTI_CURRENCY_FLAG,
  ZERO_AMOUNT_ALLOWED,
  MAX_OUTLAY,
  MAX_CHECK_AMOUNT,
  MIN_CHECK_AMOUNT,
  AP_AMOUNT_TOLERANCE,
  AR_AMOUNT_TOLERANCE,
  XTR_AMOUNT_TOLERANCE,
  PAY_AMOUNT_TOLERANCE,
  AP_PERCENT_TOLERANCE,
  AR_PERCENT_TOLERANCE,
  XTR_PERCENT_TOLERANCE,
  PAY_PERCENT_TOLERANCE,
  BANK_ACCOUNT_TYPE,
  AGENCY_LOCATION_CODE,
  START_DATE,
  END_DATE,
  ACCOUNT_HOLDER_NAME_ALT,
  ACCOUNT_HOLDER_NAME,
  CASHFLOW_DISPLAY_ORDER,
  POOLED_FLAG,
  MIN_TARGET_BALANCE,
  MAX_TARGET_BALANCE,
  EFT_USER_NUM,
  MASKED_ACCOUNT_NUM,
  MASKED_IBAN,
  INTEREST_SCHEDULE_ID,
  CASHPOOL_MIN_PAYMENT_AMT,
  CASHPOOL_MIN_RECEIPT_AMT,
  CASHPOOL_ROUND_FACTOR,
  CASHPOOL_ROUND_RULE,
  CE_AMOUNT_TOLERANCE,
  CE_PERCENT_TOLERANCE,
  ASSET_CODE_COMBINATION_ID,
  CASH_CLEARING_CCID,
  BANK_CHARGES_CCID,
  BANK_ERRORS_CCID,
  OBJECT_VERSION_NUMBER,
  NETTING_ACCT_FLAG,
  POOL_PAYMENT_METHOD_CODE,
  POOL_BANK_CHARGE_BEARER_CODE,
  POOL_PAYMENT_REASON_CODE,
  POOL_PAYMENT_REASON_COMMENTS,
  POOL_REMITTANCE_MESSAGE1,
  POOL_REMITTANCE_MESSAGE2,
  POOL_REMITTANCE_MESSAGE3,
  FX_CHARGE_CCID,
  BANK_ACCOUNT_NUM_ELECTRONIC,
  STMT_LINE_FLOAT_HANDLING_FLAG,
  AUTORECON_AP_MATCHING_ORDER,
  AUTORECON_AR_MATCHING_ORDER,
  RECON_FOREIGN_BANK_XRATE_TYPE,
  RECON_FOR_BANK_XRATE_DATE_TYPE,
  RECON_ENABLE_OI_FLAG,
  RECON_OI_FLOAT_STATUS,
  RECON_OI_CLEARED_STATUS,
  RECON_OI_MATCHING_CODE,
  RECON_OI_AMOUNT_TOLERANCE,
  RECON_OI_PERCENT_TOLERANCE,
  MANUAL_RECON_AMOUNT_TOLERANCE,
  MANUAL_RECON_PERCENT_TOLERANCE,
  RECON_AP_FOREIGN_DIFF_HANDLING,
  RECON_AR_FOREIGN_DIFF_HANDLING,
  RECON_CE_FOREIGN_DIFF_HANDLING,
  RECON_AP_TOLERANCE_DIFF_ACCT,
  RECON_CE_TOLERANCE_DIFF_ACCT,
  XTR_BANK_ACCOUNT_REFERENCE,
  AUTORECON_AP_MATCHING_ORDER2,
  AUTORECON_AR_MATCHING_ORDER2,
  AUTORECON_AR_MATCHING_ORDER3,
  AUTORECON_AR_MATCHING_ORDER4,
  AUTORECON_AR_MATCHING_ORDER5,
  FED_DO_SYMBOL,
  FED_DUNS_NUM,
  GAIN_CODE_COMBINATION_ID,
  LOSS_CODE_COMBINATION_ID,
  SEARCH_BY_PSON,
  KCA_OPERATION,
  IS_DELETED_FLG,
  KCA_SEQ_ID,
  kca_seq_date
)
(
  SELECT
    BANK_ACCOUNT_ID,
    BANK_ACCOUNT_NAME,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    LAST_UPDATE_LOGIN,
    CREATION_DATE,
    CREATED_BY,
    BANK_ACCOUNT_NUM,
    BANK_BRANCH_ID,
    BANK_ID,
    CURRENCY_CODE,
    DESCRIPTION,
    ATTRIBUTE_CATEGORY,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    REQUEST_ID,
    PROGRAM_APPLICATION_ID,
    PROGRAM_ID,
    PROGRAM_UPDATE_DATE,
    CHECK_DIGITS,
    BANK_ACCOUNT_NAME_ALT,
    ACCOUNT_HOLDER_ID,
    EFT_REQUESTER_IDENTIFIER,
    SECONDARY_ACCOUNT_REFERENCE,
    ACCOUNT_SUFFIX,
    DESCRIPTION_CODE1,
    DESCRIPTION_CODE2,
    IBAN_NUMBER,
    SHORT_ACCOUNT_NAME,
    ACCOUNT_OWNER_PARTY_ID,
    ACCOUNT_OWNER_ORG_ID,
    ACCOUNT_CLASSIFICATION,
    AP_USE_ALLOWED_FLAG,
    AR_USE_ALLOWED_FLAG,
    XTR_USE_ALLOWED_FLAG,
    PAY_USE_ALLOWED_FLAG,
    MULTI_CURRENCY_ALLOWED_FLAG,
    PAYMENT_MULTI_CURRENCY_FLAG,
    RECEIPT_MULTI_CURRENCY_FLAG,
    ZERO_AMOUNT_ALLOWED,
    MAX_OUTLAY,
    MAX_CHECK_AMOUNT,
    MIN_CHECK_AMOUNT,
    AP_AMOUNT_TOLERANCE,
    AR_AMOUNT_TOLERANCE,
    XTR_AMOUNT_TOLERANCE,
    PAY_AMOUNT_TOLERANCE,
    AP_PERCENT_TOLERANCE,
    AR_PERCENT_TOLERANCE,
    XTR_PERCENT_TOLERANCE,
    PAY_PERCENT_TOLERANCE,
    BANK_ACCOUNT_TYPE,
    AGENCY_LOCATION_CODE,
    START_DATE,
    END_DATE,
    ACCOUNT_HOLDER_NAME_ALT,
    ACCOUNT_HOLDER_NAME,
    CASHFLOW_DISPLAY_ORDER,
    POOLED_FLAG,
    MIN_TARGET_BALANCE,
    MAX_TARGET_BALANCE,
    EFT_USER_NUM,
    MASKED_ACCOUNT_NUM,
    MASKED_IBAN,
    INTEREST_SCHEDULE_ID,
    CASHPOOL_MIN_PAYMENT_AMT,
    CASHPOOL_MIN_RECEIPT_AMT,
    CASHPOOL_ROUND_FACTOR,
    CASHPOOL_ROUND_RULE,
    CE_AMOUNT_TOLERANCE,
    CE_PERCENT_TOLERANCE,
    ASSET_CODE_COMBINATION_ID,
    CASH_CLEARING_CCID,
    BANK_CHARGES_CCID,
    BANK_ERRORS_CCID,
    OBJECT_VERSION_NUMBER,
    NETTING_ACCT_FLAG,
    POOL_PAYMENT_METHOD_CODE,
    POOL_BANK_CHARGE_BEARER_CODE,
    POOL_PAYMENT_REASON_CODE,
    POOL_PAYMENT_REASON_COMMENTS,
    POOL_REMITTANCE_MESSAGE1,
    POOL_REMITTANCE_MESSAGE2,
    POOL_REMITTANCE_MESSAGE3,
    FX_CHARGE_CCID,
    BANK_ACCOUNT_NUM_ELECTRONIC,
    STMT_LINE_FLOAT_HANDLING_FLAG,
    AUTORECON_AP_MATCHING_ORDER,
    AUTORECON_AR_MATCHING_ORDER,
    RECON_FOREIGN_BANK_XRATE_TYPE,
    RECON_FOR_BANK_XRATE_DATE_TYPE,
    RECON_ENABLE_OI_FLAG,
    RECON_OI_FLOAT_STATUS,
    RECON_OI_CLEARED_STATUS,
    RECON_OI_MATCHING_CODE,
    RECON_OI_AMOUNT_TOLERANCE,
    RECON_OI_PERCENT_TOLERANCE,
    MANUAL_RECON_AMOUNT_TOLERANCE,
    MANUAL_RECON_PERCENT_TOLERANCE,
    RECON_AP_FOREIGN_DIFF_HANDLING,
    RECON_AR_FOREIGN_DIFF_HANDLING,
    RECON_CE_FOREIGN_DIFF_HANDLING,
    RECON_AP_TOLERANCE_DIFF_ACCT,
    RECON_CE_TOLERANCE_DIFF_ACCT,
    XTR_BANK_ACCOUNT_REFERENCE,
    AUTORECON_AP_MATCHING_ORDER2,
    AUTORECON_AR_MATCHING_ORDER2,
    AUTORECON_AR_MATCHING_ORDER3,
    AUTORECON_AR_MATCHING_ORDER4,
    AUTORECON_AR_MATCHING_ORDER5,
    FED_DO_SYMBOL,
    FED_DUNS_NUM,
    GAIN_CODE_COMBINATION_ID,
    LOSS_CODE_COMBINATION_ID,
    SEARCH_BY_PSON,
    KCA_OPERATION,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.ce_bank_accounts
  WHERE
    kca_operation IN ('INSERT', 'UPDATE')
    AND (BANK_ACCOUNT_ID, kca_seq_id) IN (
      SELECT
        BANK_ACCOUNT_ID,
        MAX(kca_seq_id)
      FROM bronze_bec_ods_stg.ce_bank_accounts
      WHERE
        kca_operation IN ('INSERT', 'UPDATE')
      GROUP BY
        BANK_ACCOUNT_ID
    )
);
/* Soft delete */
UPDATE silver_bec_ods.ce_bank_accounts SET IS_DELETED_FLG = 'N';
UPDATE silver_bec_ods.ce_bank_accounts SET IS_DELETED_FLG = 'Y'
WHERE
  (
    BANK_ACCOUNT_ID
  ) IN (
    SELECT
      BANK_ACCOUNT_ID
    FROM bec_raw_dl_ext.ce_bank_accounts
    WHERE
      (BANK_ACCOUNT_ID, KCA_SEQ_ID) IN (
        SELECT
          BANK_ACCOUNT_ID,
          MAX(KCA_SEQ_ID) AS KCA_SEQ_ID
        FROM bec_raw_dl_ext.ce_bank_accounts
        GROUP BY
          BANK_ACCOUNT_ID
      )
      AND kca_operation = 'DELETE'
  );
UPDATE bec_etl_ctrl.batch_ods_info SET last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'ce_bank_accounts';