TRUNCATE table silver_bec_ods.XXBEC_INVENTORY_VALUE_RPT;
INSERT INTO silver_bec_ods.XXBEC_INVENTORY_VALUE_RPT (
  AS_OF_DATE,
  INV_CATEGORY,
  INV_SUBCATEGORY,
  SUBINVENTORY,
  LOCATOR,
  PART_NUMBER,
  DESCRIPTION,
  INVENTORY_ITEM_STATUS_CODE,
  CATEGORY_SEGMENT1,
  CATEGORY_SEGMENT2,
  CATEGORY,
  PRIMARY_UOM_CODE,
  QUANTITY,
  ITEM_COST,
  EXTENDED_COST,
  ORGANIZATION_ID,
  ORGANIZATION_NAME,
  ORGANIZATION_CODE,
  VMI_FLAG,
  OWNING_ORGANIZATION_ID,
  SUBINVENTORY_TYPE,
  MATERIAL_COST,
  MATERIAL_OVERHEAD_COST,
  RESOURCE_COST,
  OUTSIDE_PROCESSING_COST,
  OVERHEAD_COST,
  EXT_MATERIAL_COST,
  EXT_MATERIAL_OVERHEAD_COST,
  EXT_RESOURCE_COST,
  EXT_OUTSIDE_PROCESSING_COST,
  EXT_OVERHEAD_COST,
  LOCATOR_ID,
  kca_operation,
  is_deleted_flg,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    AS_OF_DATE,
    INV_CATEGORY,
    INV_SUBCATEGORY,
    SUBINVENTORY,
    LOCATOR,
    PART_NUMBER,
    DESCRIPTION,
    INVENTORY_ITEM_STATUS_CODE,
    CATEGORY_SEGMENT1,
    CATEGORY_SEGMENT2,
    CATEGORY,
    PRIMARY_UOM_CODE,
    QUANTITY,
    ITEM_COST,
    EXTENDED_COST,
    ORGANIZATION_ID,
    ORGANIZATION_NAME,
    ORGANIZATION_CODE,
    VMI_FLAG,
    OWNING_ORGANIZATION_ID,
    SUBINVENTORY_TYPE,
    MATERIAL_COST,
    MATERIAL_OVERHEAD_COST,
    RESOURCE_COST,
    OUTSIDE_PROCESSING_COST,
    OVERHEAD_COST,
    EXT_MATERIAL_COST,
    EXT_MATERIAL_OVERHEAD_COST,
    EXT_RESOURCE_COST,
    EXT_OUTSIDE_PROCESSING_COST,
    EXT_OVERHEAD_COST,
    LOCATOR_ID,
    kca_operation,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.XXBEC_INVENTORY_VALUE_RPT
);
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'xxbec_inventory_value_rpt';