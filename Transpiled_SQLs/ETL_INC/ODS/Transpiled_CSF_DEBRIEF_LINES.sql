/* Delete Records */
DELETE FROM silver_bec_ods.CSF_DEBRIEF_LINES
WHERE
  (
    COALESCE(DEBRIEF_LINE_ID, 0)
  ) IN (
    SELECT
      COALESCE(stg.DEBRIEF_LINE_ID, 0) AS DEBRIEF_LINE_ID
    FROM silver_bec_ods.CSF_DEBRIEF_LINES AS ods, bronze_bec_ods_stg.CSF_DEBRIEF_LINES AS stg
    WHERE
      COALESCE(ods.DEBRIEF_LINE_ID, 0) = COALESCE(stg.DEBRIEF_LINE_ID, 0)
      AND stg.kca_operation IN ('INSERT', 'UPDATE')
  );
/* Insert records */
INSERT INTO silver_bec_ods.CSF_DEBRIEF_LINES (
  DEBRIEF_LINE_ID,
  DEBRIEF_HEADER_ID,
  DEBRIEF_LINE_NUMBER,
  SERVICE_DATE,
  TXN_BILLING_TYPE_ID,
  INVENTORY_ITEM_ID,
  ISSUING_INVENTORY_ORG_ID,
  RECEIVING_INVENTORY_ORG_ID,
  ISSUING_SUB_INVENTORY_CODE,
  RECEIVING_SUB_INVENTORY_CODE,
  ISSUING_LOCATOR_ID,
  RECEIVING_LOCATOR_ID,
  PARENT_PRODUCT_ID,
  REMOVED_PRODUCT_ID,
  STATUS_OF_RECEIVED_PART,
  ITEM_SERIAL_NUMBER,
  ITEM_REVISION,
  ITEM_LOTNUMBER,
  UOM_CODE,
  QUANTITY,
  RMA_HEADER_ID,
  DISPOSITION_CODE,
  MATERIAL_REASON_CODE,
  LABOR_REASON_CODE,
  EXPENSE_REASON_CODE,
  LABOR_START_DATE,
  LABOR_END_DATE,
  STARTING_MILEAGE,
  ENDING_MILEAGE,
  EXPENSE_AMOUNT,
  CURRENCY_CODE,
  DEBRIEF_LINE_STATUS_ID,
  CHANNEL_CODE,
  CHARGE_UPLOAD_STATUS,
  CHARGE_UPLOAD_MSG_CODE,
  CHARGE_UPLOAD_MESSAGE,
  IB_UPDATE_STATUS,
  IB_UPDATE_MSG_CODE,
  IB_UPDATE_MESSAGE,
  SPARE_UPDATE_STATUS,
  SPARE_UPDATE_MSG_CODE,
  SPARE_UPDATE_MESSAGE,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  LAST_UPDATE_LOGIN,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  ATTRIBUTE_CATEGORY,
  SECURITY_GROUP_ID,
  UPG_ORIG_SYSTEM_REF,
  UPG_ORIG_SYSTEM_REF_ID,
  BUSINESS_PROCESS_ID,
  RETURN_REASON_CODE,
  INSTANCE_ID,
  STATISTICS_UPDATED,
  ERROR_TEXT,
  TRANSACTION_TYPE_ID,
  RETURN_DATE,
  OBJECT_VERSION_NUMBER,
  ITEM_OPERATIONAL_STATUS_CODE,
  MATERIAL_TRANSACTION_ID,
  USAGE_TYPE,
  RETURN_ORGANIZATION_ID,
  RETURN_SUBINVENTORY_NAME,
  CARRIER_CODE,
  SHIPPING_METHOD,
  SHIPPING_NUMBER,
  WAYBILL,
  EXPENDITURE_ORG_ID,
  PROJECT_ID,
  PROJECT_TASK_ID,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_ID,
  kca_seq_date
)
(
  SELECT
    DEBRIEF_LINE_ID,
    DEBRIEF_HEADER_ID,
    DEBRIEF_LINE_NUMBER,
    SERVICE_DATE,
    TXN_BILLING_TYPE_ID,
    INVENTORY_ITEM_ID,
    ISSUING_INVENTORY_ORG_ID,
    RECEIVING_INVENTORY_ORG_ID,
    ISSUING_SUB_INVENTORY_CODE,
    RECEIVING_SUB_INVENTORY_CODE,
    ISSUING_LOCATOR_ID,
    RECEIVING_LOCATOR_ID,
    PARENT_PRODUCT_ID,
    REMOVED_PRODUCT_ID,
    STATUS_OF_RECEIVED_PART,
    ITEM_SERIAL_NUMBER,
    ITEM_REVISION,
    ITEM_LOTNUMBER,
    UOM_CODE,
    QUANTITY,
    RMA_HEADER_ID,
    DISPOSITION_CODE,
    MATERIAL_REASON_CODE,
    LABOR_REASON_CODE,
    EXPENSE_REASON_CODE,
    LABOR_START_DATE,
    LABOR_END_DATE,
    STARTING_MILEAGE,
    ENDING_MILEAGE,
    EXPENSE_AMOUNT,
    CURRENCY_CODE,
    DEBRIEF_LINE_STATUS_ID,
    CHANNEL_CODE,
    CHARGE_UPLOAD_STATUS,
    CHARGE_UPLOAD_MSG_CODE,
    CHARGE_UPLOAD_MESSAGE,
    IB_UPDATE_STATUS,
    IB_UPDATE_MSG_CODE,
    IB_UPDATE_MESSAGE,
    SPARE_UPDATE_STATUS,
    SPARE_UPDATE_MSG_CODE,
    SPARE_UPDATE_MESSAGE,
    CREATED_BY,
    CREATION_DATE,
    LAST_UPDATED_BY,
    LAST_UPDATE_DATE,
    LAST_UPDATE_LOGIN,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    ATTRIBUTE_CATEGORY,
    SECURITY_GROUP_ID,
    UPG_ORIG_SYSTEM_REF,
    UPG_ORIG_SYSTEM_REF_ID,
    BUSINESS_PROCESS_ID,
    RETURN_REASON_CODE,
    INSTANCE_ID,
    STATISTICS_UPDATED,
    ERROR_TEXT,
    TRANSACTION_TYPE_ID,
    RETURN_DATE,
    OBJECT_VERSION_NUMBER,
    ITEM_OPERATIONAL_STATUS_CODE,
    MATERIAL_TRANSACTION_ID,
    USAGE_TYPE,
    RETURN_ORGANIZATION_ID,
    RETURN_SUBINVENTORY_NAME,
    CARRIER_CODE,
    SHIPPING_METHOD,
    SHIPPING_NUMBER,
    WAYBILL,
    EXPENDITURE_ORG_ID,
    PROJECT_ID,
    PROJECT_TASK_ID,
    KCA_OPERATION,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.CSF_DEBRIEF_LINES
  WHERE
    kca_operation IN ('INSERT', 'UPDATE')
    AND (COALESCE(DEBRIEF_LINE_ID, 0), kca_seq_ID) IN (
      SELECT
        COALESCE(DEBRIEF_LINE_ID, 0) AS DEBRIEF_LINE_ID,
        MAX(kca_seq_ID) AS kca_seq_ID
      FROM bronze_bec_ods_stg.CSF_DEBRIEF_LINES
      WHERE
        kca_operation IN ('INSERT', 'UPDATE')
      GROUP BY
        COALESCE(DEBRIEF_LINE_ID, 0)
    )
);
/* Soft delete */
UPDATE silver_bec_ods.CSF_DEBRIEF_LINES SET IS_DELETED_FLG = 'N';
UPDATE silver_bec_ods.CSF_DEBRIEF_LINES SET IS_DELETED_FLG = 'Y'
WHERE
  (
    DEBRIEF_LINE_ID
  ) IN (
    SELECT
      DEBRIEF_LINE_ID
    FROM bec_raw_dl_ext.CSF_DEBRIEF_LINES
    WHERE
      (DEBRIEF_LINE_ID, KCA_SEQ_ID) IN (
        SELECT
          DEBRIEF_LINE_ID,
          MAX(KCA_SEQ_ID) AS KCA_SEQ_ID
        FROM bec_raw_dl_ext.CSF_DEBRIEF_LINES
        GROUP BY
          DEBRIEF_LINE_ID
      )
      AND kca_operation = 'DELETE'
  );
UPDATE bec_etl_ctrl.batch_ods_info SET last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'csf_debrief_lines';