/* Delete Records */
DELETE FROM silver_bec_ods.WIP_OPERATION_RESOURCES
WHERE
  (COALESCE(WIP_ENTITY_ID, 0), COALESCE(OPERATION_SEQ_NUM, 0), COALESCE(RESOURCE_SEQ_NUM, 0), COALESCE(REPETITIVE_SCHEDULE_ID, 0)) IN (
    SELECT
      COALESCE(stg.WIP_ENTITY_ID, 0) AS WIP_ENTITY_ID,
      COALESCE(stg.OPERATION_SEQ_NUM, 0) AS OPERATION_SEQ_NUM,
      COALESCE(stg.RESOURCE_SEQ_NUM, 0) AS RESOURCE_SEQ_NUM,
      COALESCE(stg.REPETITIVE_SCHEDULE_ID, 0) AS REPETITIVE_SCHEDULE_ID
    FROM silver_bec_ods.WIP_OPERATION_RESOURCES AS ods, bronze_bec_ods_stg.WIP_OPERATION_RESOURCES AS stg
    WHERE
      COALESCE(ods.WIP_ENTITY_ID, 0) = COALESCE(stg.WIP_ENTITY_ID, 0)
      AND COALESCE(ods.OPERATION_SEQ_NUM, 0) = COALESCE(stg.OPERATION_SEQ_NUM, 0)
      AND COALESCE(ods.RESOURCE_SEQ_NUM, 0) = COALESCE(stg.RESOURCE_SEQ_NUM, 0)
      AND COALESCE(ods.REPETITIVE_SCHEDULE_ID, 0) = COALESCE(stg.REPETITIVE_SCHEDULE_ID, 0)
      AND stg.kca_operation IN ('INSERT', 'UPDATE')
  );
/* Insert records */
INSERT INTO silver_bec_ods.WIP_OPERATION_RESOURCES (
  WIP_ENTITY_ID,
  OPERATION_SEQ_NUM,
  RESOURCE_SEQ_NUM,
  ORGANIZATION_ID,
  REPETITIVE_SCHEDULE_ID,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  REQUEST_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  RESOURCE_ID,
  UOM_CODE,
  BASIS_TYPE,
  USAGE_RATE_OR_AMOUNT,
  ACTIVITY_ID,
  SCHEDULED_FLAG,
  ASSIGNED_UNITS,
  AUTOCHARGE_TYPE,
  STANDARD_RATE_FLAG,
  APPLIED_RESOURCE_UNITS,
  APPLIED_RESOURCE_VALUE,
  START_DATE,
  COMPLETION_DATE,
  ATTRIBUTE_CATEGORY,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  RELIEVED_RES_COMPLETION_UNITS,
  RELIEVED_RES_SCRAP_UNITS,
  RELIEVED_RES_COMPLETION_VALUE,
  RELIEVED_RES_SCRAP_VALUE,
  RELIEVED_VARIANCE_VALUE,
  TEMP_RELIEVED_VALUE,
  RELIEVED_RES_FINAL_COMP_UNITS,
  DEPARTMENT_ID,
  PHANTOM_FLAG,
  PHANTOM_OP_SEQ_NUM,
  PHANTOM_ITEM_ID,
  SCHEDULE_SEQ_NUM,
  SUBSTITUTE_GROUP_NUM,
  REPLACEMENT_GROUP_NUM,
  PRINCIPLE_FLAG,
  SETUP_ID,
  PARENT_RESOURCE_SEQ,
  BATCH_ID,
  RELIEVED_RES_UNITS,
  RELIEVED_RES_VALUE,
  MAXIMUM_ASSIGNED_UNITS,
  FIRM_FLAG,
  GROUP_SEQUENCE_ID,
  GROUP_SEQUENCE_NUMBER,
  ACTUAL_START_DATE,
  ACTUAL_COMPLETION_DATE,
  PROJECTED_COMPLETION_DATE,
  kca_operation,
  is_deleted_flg,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    WIP_ENTITY_ID,
    OPERATION_SEQ_NUM,
    RESOURCE_SEQ_NUM,
    ORGANIZATION_ID,
    REPETITIVE_SCHEDULE_ID,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    REQUEST_ID,
    PROGRAM_APPLICATION_ID,
    PROGRAM_ID,
    PROGRAM_UPDATE_DATE,
    RESOURCE_ID,
    UOM_CODE,
    BASIS_TYPE,
    USAGE_RATE_OR_AMOUNT,
    ACTIVITY_ID,
    SCHEDULED_FLAG,
    ASSIGNED_UNITS,
    AUTOCHARGE_TYPE,
    STANDARD_RATE_FLAG,
    APPLIED_RESOURCE_UNITS,
    APPLIED_RESOURCE_VALUE,
    START_DATE,
    COMPLETION_DATE,
    ATTRIBUTE_CATEGORY,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    RELIEVED_RES_COMPLETION_UNITS,
    RELIEVED_RES_SCRAP_UNITS,
    RELIEVED_RES_COMPLETION_VALUE,
    RELIEVED_RES_SCRAP_VALUE,
    RELIEVED_VARIANCE_VALUE,
    TEMP_RELIEVED_VALUE,
    RELIEVED_RES_FINAL_COMP_UNITS,
    DEPARTMENT_ID,
    PHANTOM_FLAG,
    PHANTOM_OP_SEQ_NUM,
    PHANTOM_ITEM_ID,
    SCHEDULE_SEQ_NUM,
    SUBSTITUTE_GROUP_NUM,
    REPLACEMENT_GROUP_NUM,
    PRINCIPLE_FLAG,
    SETUP_ID,
    PARENT_RESOURCE_SEQ,
    BATCH_ID,
    RELIEVED_RES_UNITS,
    RELIEVED_RES_VALUE,
    MAXIMUM_ASSIGNED_UNITS,
    FIRM_FLAG,
    GROUP_SEQUENCE_ID,
    GROUP_SEQUENCE_NUMBER,
    ACTUAL_START_DATE,
    ACTUAL_COMPLETION_DATE,
    PROJECTED_COMPLETION_DATE,
    kca_operation,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.WIP_OPERATION_RESOURCES
  WHERE
    kca_operation IN ('INSERT', 'UPDATE')
    AND (COALESCE(WIP_ENTITY_ID, 0), COALESCE(OPERATION_SEQ_NUM, 0), COALESCE(RESOURCE_SEQ_NUM, 0), COALESCE(REPETITIVE_SCHEDULE_ID, 0), kca_seq_id) IN (
      SELECT
        COALESCE(WIP_ENTITY_ID, 0) AS WIP_ENTITY_ID,
        COALESCE(OPERATION_SEQ_NUM, 0) AS OPERATION_SEQ_NUM,
        COALESCE(RESOURCE_SEQ_NUM, 0) AS RESOURCE_SEQ_NUM,
        COALESCE(REPETITIVE_SCHEDULE_ID, 0) AS REPETITIVE_SCHEDULE_ID,
        MAX(kca_seq_id)
      FROM bronze_bec_ods_stg.WIP_OPERATION_RESOURCES
      WHERE
        kca_operation IN ('INSERT', 'UPDATE')
      GROUP BY
        COALESCE(WIP_ENTITY_ID, 0),
        COALESCE(OPERATION_SEQ_NUM, 0),
        COALESCE(RESOURCE_SEQ_NUM, 0),
        COALESCE(REPETITIVE_SCHEDULE_ID, 0)
    )
);
/* Soft delete */
UPDATE silver_bec_ods.WIP_OPERATION_RESOURCES SET IS_DELETED_FLG = 'N';
UPDATE silver_bec_ods.WIP_OPERATION_RESOURCES SET IS_DELETED_FLG = 'Y'
WHERE
  (COALESCE(WIP_ENTITY_ID, 0), COALESCE(OPERATION_SEQ_NUM, 0), COALESCE(RESOURCE_SEQ_NUM, 0), COALESCE(REPETITIVE_SCHEDULE_ID, 0)) IN (
    SELECT
      COALESCE(WIP_ENTITY_ID, 0),
      COALESCE(OPERATION_SEQ_NUM, 0),
      COALESCE(RESOURCE_SEQ_NUM, 0),
      COALESCE(REPETITIVE_SCHEDULE_ID, 0)
    FROM bec_raw_dl_ext.WIP_OPERATION_RESOURCES
    WHERE
      (COALESCE(WIP_ENTITY_ID, 0), COALESCE(OPERATION_SEQ_NUM, 0), COALESCE(RESOURCE_SEQ_NUM, 0), COALESCE(REPETITIVE_SCHEDULE_ID, 0), KCA_SEQ_ID) IN (
        SELECT
          COALESCE(WIP_ENTITY_ID, 0),
          COALESCE(OPERATION_SEQ_NUM, 0),
          COALESCE(RESOURCE_SEQ_NUM, 0),
          COALESCE(REPETITIVE_SCHEDULE_ID, 0),
          MAX(KCA_SEQ_ID) AS KCA_SEQ_ID
        FROM bec_raw_dl_ext.WIP_OPERATION_RESOURCES
        GROUP BY
          COALESCE(WIP_ENTITY_ID, 0),
          COALESCE(OPERATION_SEQ_NUM, 0),
          COALESCE(RESOURCE_SEQ_NUM, 0),
          COALESCE(REPETITIVE_SCHEDULE_ID, 0)
      )
      AND kca_operation = 'DELETE'
  );
UPDATE bec_etl_ctrl.batch_ods_info SET last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'wip_operation_resources';