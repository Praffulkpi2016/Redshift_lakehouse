/* Delete Records */
DELETE FROM silver_bec_ods.FA_ADDITIONS_B
WHERE
  (
    COALESCE(ASSET_ID, 0)
  ) IN (
    SELECT
      COALESCE(stg.ASSET_ID, 0) AS ASSET_ID
    FROM silver_bec_ods.FA_ADDITIONS_B AS ods, bronze_bec_ods_stg.FA_ADDITIONS_B AS stg
    WHERE
      COALESCE(ods.ASSET_ID, 0) = COALESCE(stg.ASSET_ID, 0)
      AND stg.kca_operation IN ('INSERT', 'UPDATE')
  );
/* Insert records */
INSERT INTO silver_bec_ods.FA_ADDITIONS_B (
  ASSET_ID,
  ASSET_NUMBER,
  ASSET_KEY_CCID,
  CURRENT_UNITS,
  ASSET_TYPE,
  TAG_NUMBER,
  ASSET_CATEGORY_ID,
  PARENT_ASSET_ID,
  MANUFACTURER_NAME,
  SERIAL_NUMBER,
  MODEL_NUMBER,
  PROPERTY_TYPE_CODE,
  PROPERTY_1245_1250_CODE,
  IN_USE_FLAG,
  OWNED_LEASED,
  NEW_USED,
  UNIT_ADJUSTMENT_FLAG,
  ADD_COST_JE_FLAG,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  ATTRIBUTE16,
  ATTRIBUTE17,
  ATTRIBUTE18,
  ATTRIBUTE19,
  ATTRIBUTE20,
  ATTRIBUTE21,
  ATTRIBUTE22,
  ATTRIBUTE23,
  ATTRIBUTE24,
  ATTRIBUTE25,
  ATTRIBUTE26,
  ATTRIBUTE27,
  ATTRIBUTE28,
  ATTRIBUTE29,
  ATTRIBUTE30,
  ATTRIBUTE_CATEGORY_CODE,
  CONTEXT,
  LEASE_ID,
  INVENTORIAL,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATE_LOGIN,
  GLOBAL_ATTRIBUTE1,
  GLOBAL_ATTRIBUTE2,
  GLOBAL_ATTRIBUTE3,
  GLOBAL_ATTRIBUTE4,
  GLOBAL_ATTRIBUTE5,
  GLOBAL_ATTRIBUTE6,
  GLOBAL_ATTRIBUTE7,
  GLOBAL_ATTRIBUTE8,
  GLOBAL_ATTRIBUTE9,
  GLOBAL_ATTRIBUTE10,
  GLOBAL_ATTRIBUTE11,
  GLOBAL_ATTRIBUTE12,
  GLOBAL_ATTRIBUTE13,
  GLOBAL_ATTRIBUTE14,
  GLOBAL_ATTRIBUTE15,
  GLOBAL_ATTRIBUTE16,
  GLOBAL_ATTRIBUTE17,
  GLOBAL_ATTRIBUTE18,
  GLOBAL_ATTRIBUTE19,
  GLOBAL_ATTRIBUTE20,
  GLOBAL_ATTRIBUTE_CATEGORY,
  COMMITMENT,
  INVESTMENT_LAW,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    ASSET_ID,
    ASSET_NUMBER,
    ASSET_KEY_CCID,
    CURRENT_UNITS,
    ASSET_TYPE,
    TAG_NUMBER,
    ASSET_CATEGORY_ID,
    PARENT_ASSET_ID,
    MANUFACTURER_NAME,
    SERIAL_NUMBER,
    MODEL_NUMBER,
    PROPERTY_TYPE_CODE,
    PROPERTY_1245_1250_CODE,
    IN_USE_FLAG,
    OWNED_LEASED,
    NEW_USED,
    UNIT_ADJUSTMENT_FLAG,
    ADD_COST_JE_FLAG,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    ATTRIBUTE16,
    ATTRIBUTE17,
    ATTRIBUTE18,
    ATTRIBUTE19,
    ATTRIBUTE20,
    ATTRIBUTE21,
    ATTRIBUTE22,
    ATTRIBUTE23,
    ATTRIBUTE24,
    ATTRIBUTE25,
    ATTRIBUTE26,
    ATTRIBUTE27,
    ATTRIBUTE28,
    ATTRIBUTE29,
    ATTRIBUTE30,
    ATTRIBUTE_CATEGORY_CODE,
    CONTEXT,
    LEASE_ID,
    INVENTORIAL,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    CREATED_BY,
    CREATION_DATE,
    LAST_UPDATE_LOGIN,
    GLOBAL_ATTRIBUTE1,
    GLOBAL_ATTRIBUTE2,
    GLOBAL_ATTRIBUTE3,
    GLOBAL_ATTRIBUTE4,
    GLOBAL_ATTRIBUTE5,
    GLOBAL_ATTRIBUTE6,
    GLOBAL_ATTRIBUTE7,
    GLOBAL_ATTRIBUTE8,
    GLOBAL_ATTRIBUTE9,
    GLOBAL_ATTRIBUTE10,
    GLOBAL_ATTRIBUTE11,
    GLOBAL_ATTRIBUTE12,
    GLOBAL_ATTRIBUTE13,
    GLOBAL_ATTRIBUTE14,
    GLOBAL_ATTRIBUTE15,
    GLOBAL_ATTRIBUTE16,
    GLOBAL_ATTRIBUTE17,
    GLOBAL_ATTRIBUTE18,
    GLOBAL_ATTRIBUTE19,
    GLOBAL_ATTRIBUTE20,
    GLOBAL_ATTRIBUTE_CATEGORY,
    COMMITMENT,
    INVESTMENT_LAW,
    KCA_OPERATION,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.FA_ADDITIONS_B
  WHERE
    kca_operation IN ('INSERT', 'UPDATE')
    AND (COALESCE(ASSET_ID, 0), kca_seq_id) IN (
      SELECT
        COALESCE(ASSET_ID, 0) AS ASSET_ID,
        MAX(kca_seq_id)
      FROM bronze_bec_ods_stg.FA_ADDITIONS_B
      WHERE
        kca_operation IN ('INSERT', 'UPDATE')
      GROUP BY
        COALESCE(ASSET_ID, 0)
    )
);
/* Soft delete */
UPDATE silver_bec_ods.FA_ADDITIONS_B SET IS_DELETED_FLG = 'N';
UPDATE silver_bec_ods.FA_ADDITIONS_B SET IS_DELETED_FLG = 'Y'
WHERE
  (
    ASSET_ID
  ) IN (
    SELECT
      ASSET_ID
    FROM bec_raw_dl_ext.FA_ADDITIONS_B
    WHERE
      (ASSET_ID, KCA_SEQ_ID) IN (
        SELECT
          ASSET_ID,
          MAX(KCA_SEQ_ID) AS KCA_SEQ_ID
        FROM bec_raw_dl_ext.FA_ADDITIONS_B
        GROUP BY
          ASSET_ID
      )
      AND kca_operation = 'DELETE'
  );
UPDATE bec_etl_ctrl.batch_ods_info SET last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'fa_additions_b';