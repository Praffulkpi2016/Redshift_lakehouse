TRUNCATE table
	table bronze_bec_ods_stg.CS_TRANSACTION_TYPES_B;
INSERT INTO bronze_bec_ods_stg.CS_TRANSACTION_TYPES_B (
  TRANSACTION_TYPE_ID,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  SEEDED_FLAG,
  REVISION_FLAG,
  END_DATE_ACTIVE,
  START_DATE_ACTIVE,
  INSTALLED_CP_STATUS_ID,
  INSTALLED_STATUS_CODE,
  INSTALLED_CP_RETURN_REQUIRED,
  BILLING_FLAG,
  NEW_CP_STATUS_ID,
  NEW_CP_STATUS_CODE,
  TRANSFER_SERVICE,
  NEW_CP_RETURN_REQUIRED,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  CONTEXT,
  NO_CHARGE_FLAG,
  MOVE_COUNTERS_FLAG,
  OBJECT_VERSION_NUMBER,
  DEPOT_REPAIR_FLAG,
  SECURITY_GROUP_ID,
  LINE_ORDER_CATEGORY_CODE,
  INTERFACE_TO_OE_FLAG,
  CREATE_COST_FLAG,
  CREATE_CHARGE_FLAG,
  TRAVEL_FLAG,
  CALCULATE_PRICE_FLAG,
  ZD_EDITION_NAME,
  ZD_SYNC,
  UPDATE_TASK_ACTUALS_FLAG,
  KCA_OPERATION,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    TRANSACTION_TYPE_ID,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    SEEDED_FLAG,
    REVISION_FLAG,
    END_DATE_ACTIVE,
    START_DATE_ACTIVE,
    INSTALLED_CP_STATUS_ID,
    INSTALLED_STATUS_CODE,
    INSTALLED_CP_RETURN_REQUIRED,
    BILLING_FLAG,
    NEW_CP_STATUS_ID,
    NEW_CP_STATUS_CODE,
    TRANSFER_SERVICE,
    NEW_CP_RETURN_REQUIRED,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    CONTEXT,
    NO_CHARGE_FLAG,
    MOVE_COUNTERS_FLAG,
    OBJECT_VERSION_NUMBER,
    DEPOT_REPAIR_FLAG,
    SECURITY_GROUP_ID,
    LINE_ORDER_CATEGORY_CODE,
    INTERFACE_TO_OE_FLAG,
    CREATE_COST_FLAG,
    CREATE_CHARGE_FLAG,
    TRAVEL_FLAG,
    CALCULATE_PRICE_FLAG,
    ZD_EDITION_NAME,
    ZD_SYNC,
    UPDATE_TASK_ACTUALS_FLAG,
    KCA_OPERATION,
    kca_seq_id,
    kca_seq_date
  FROM bec_raw_dl_ext.CS_TRANSACTION_TYPES_B
  WHERE
    kca_operation <> 'DELETE'
    AND COALESCE(kca_seq_id, '') <> ''
    AND (COALESCE(TRANSACTION_TYPE_ID, 0), kca_seq_id) IN (
      SELECT
        COALESCE(TRANSACTION_TYPE_ID, 0) AS TRANSACTION_TYPE_ID,
        MAX(kca_seq_id) AS kca_seq_id
      FROM bec_raw_dl_ext.CS_TRANSACTION_TYPES_B
      WHERE
        kca_operation <> 'DELETE' AND COALESCE(kca_seq_id, '') <> ''
      GROUP BY
        COALESCE(TRANSACTION_TYPE_ID, 0)
    )
    AND kca_seq_date > (
      SELECT
        (
          executebegints - prune_days
        )
      FROM bec_etl_ctrl.batch_ods_info
      WHERE
        ods_table_name = 'cs_transaction_types_b'
    )
);