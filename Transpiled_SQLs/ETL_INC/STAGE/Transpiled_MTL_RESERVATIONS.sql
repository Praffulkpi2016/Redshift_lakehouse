TRUNCATE table bronze_bec_ods_stg.mtl_reservations;
INSERT INTO bronze_bec_ods_stg.mtl_reservations (
  RESERVATION_ID,
  REQUIREMENT_DATE,
  ORGANIZATION_ID,
  INVENTORY_ITEM_ID,
  DEMAND_SOURCE_TYPE_ID,
  DEMAND_SOURCE_NAME,
  DEMAND_SOURCE_HEADER_ID,
  DEMAND_SOURCE_LINE_ID,
  DEMAND_SOURCE_DELIVERY,
  PRIMARY_UOM_CODE,
  PRIMARY_UOM_ID,
  RESERVATION_UOM_CODE,
  RESERVATION_UOM_ID,
  RESERVATION_QUANTITY,
  PRIMARY_RESERVATION_QUANTITY,
  AUTODETAIL_GROUP_ID,
  EXTERNAL_SOURCE_CODE,
  EXTERNAL_SOURCE_LINE_ID,
  SUPPLY_SOURCE_TYPE_ID,
  SUPPLY_SOURCE_HEADER_ID,
  SUPPLY_SOURCE_LINE_ID,
  SUPPLY_SOURCE_LINE_DETAIL,
  SUPPLY_SOURCE_NAME,
  REVISION,
  SUBINVENTORY_CODE,
  SUBINVENTORY_ID,
  LOCATOR_ID,
  LOT_NUMBER,
  LOT_NUMBER_ID,
  SERIAL_NUMBER,
  SERIAL_NUMBER_ID,
  PARTIAL_QUANTITIES_ALLOWED,
  AUTO_DETAILED,
  PICK_SLIP_NUMBER,
  LPN_ID,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  REQUEST_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  ATTRIBUTE_CATEGORY,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  ATTRIBUTE6,
  ATTRIBUTE7,
  ATTRIBUTE8,
  ATTRIBUTE9,
  ATTRIBUTE10,
  ATTRIBUTE11,
  ATTRIBUTE12,
  ATTRIBUTE13,
  ATTRIBUTE14,
  ATTRIBUTE15,
  SHIP_READY_FLAG,
  N_COLUMN1,
  DETAILED_QUANTITY,
  COST_GROUP_ID,
  CONTAINER_LPN_ID,
  STAGED_FLAG,
  SECONDARY_DETAILED_QUANTITY,
  SECONDARY_RESERVATION_QUANTITY,
  SECONDARY_UOM_CODE,
  SECONDARY_UOM_ID,
  CROSSDOCK_FLAG,
  CROSSDOCK_CRITERIA_ID,
  DEMAND_SOURCE_LINE_DETAIL,
  SERIAL_RESERVATION_QUANTITY,
  SUPPLY_RECEIPT_DATE,
  DEMAND_SHIP_DATE,
  EXCEPTION_CODE,
  ORIG_SUPPLY_SOURCE_TYPE_ID,
  ORIG_SUPPLY_SOURCE_HEADER_ID,
  ORIG_SUPPLY_SOURCE_LINE_ID,
  ORIG_SUPPLY_SOURCE_LINE_DETAIL,
  ORIG_DEMAND_SOURCE_TYPE_ID,
  ORIG_DEMAND_SOURCE_HEADER_ID,
  ORIG_DEMAND_SOURCE_LINE_ID,
  ORIG_DEMAND_SOURCE_LINE_DETAIL,
  PROJECT_ID,
  TASK_ID,
  MCC_CODE,
  KCA_OPERATION,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    RESERVATION_ID,
    REQUIREMENT_DATE,
    ORGANIZATION_ID,
    INVENTORY_ITEM_ID,
    DEMAND_SOURCE_TYPE_ID,
    DEMAND_SOURCE_NAME,
    DEMAND_SOURCE_HEADER_ID,
    DEMAND_SOURCE_LINE_ID,
    DEMAND_SOURCE_DELIVERY,
    PRIMARY_UOM_CODE,
    PRIMARY_UOM_ID,
    RESERVATION_UOM_CODE,
    RESERVATION_UOM_ID,
    RESERVATION_QUANTITY,
    PRIMARY_RESERVATION_QUANTITY,
    AUTODETAIL_GROUP_ID,
    EXTERNAL_SOURCE_CODE,
    EXTERNAL_SOURCE_LINE_ID,
    SUPPLY_SOURCE_TYPE_ID,
    SUPPLY_SOURCE_HEADER_ID,
    SUPPLY_SOURCE_LINE_ID,
    SUPPLY_SOURCE_LINE_DETAIL,
    SUPPLY_SOURCE_NAME,
    REVISION,
    SUBINVENTORY_CODE,
    SUBINVENTORY_ID,
    LOCATOR_ID,
    LOT_NUMBER,
    LOT_NUMBER_ID,
    SERIAL_NUMBER,
    SERIAL_NUMBER_ID,
    PARTIAL_QUANTITIES_ALLOWED,
    AUTO_DETAILED,
    PICK_SLIP_NUMBER,
    LPN_ID,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    REQUEST_ID,
    PROGRAM_APPLICATION_ID,
    PROGRAM_ID,
    PROGRAM_UPDATE_DATE,
    ATTRIBUTE_CATEGORY,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    ATTRIBUTE6,
    ATTRIBUTE7,
    ATTRIBUTE8,
    ATTRIBUTE9,
    ATTRIBUTE10,
    ATTRIBUTE11,
    ATTRIBUTE12,
    ATTRIBUTE13,
    ATTRIBUTE14,
    ATTRIBUTE15,
    SHIP_READY_FLAG,
    N_COLUMN1,
    DETAILED_QUANTITY,
    COST_GROUP_ID,
    CONTAINER_LPN_ID,
    STAGED_FLAG,
    SECONDARY_DETAILED_QUANTITY,
    SECONDARY_RESERVATION_QUANTITY,
    SECONDARY_UOM_CODE,
    SECONDARY_UOM_ID,
    CROSSDOCK_FLAG,
    CROSSDOCK_CRITERIA_ID,
    DEMAND_SOURCE_LINE_DETAIL,
    SERIAL_RESERVATION_QUANTITY,
    SUPPLY_RECEIPT_DATE,
    DEMAND_SHIP_DATE,
    EXCEPTION_CODE,
    ORIG_SUPPLY_SOURCE_TYPE_ID,
    ORIG_SUPPLY_SOURCE_HEADER_ID,
    ORIG_SUPPLY_SOURCE_LINE_ID,
    ORIG_SUPPLY_SOURCE_LINE_DETAIL,
    ORIG_DEMAND_SOURCE_TYPE_ID,
    ORIG_DEMAND_SOURCE_HEADER_ID,
    ORIG_DEMAND_SOURCE_LINE_ID,
    ORIG_DEMAND_SOURCE_LINE_DETAIL,
    PROJECT_ID,
    TASK_ID,
    MCC_CODE,
    KCA_OPERATION,
    kca_seq_id,
    kca_seq_date
  FROM bec_raw_dl_ext.mtl_reservations
  WHERE
    kca_operation <> 'DELETE'
    AND COALESCE(kca_seq_id, '') <> ''
    AND (COALESCE(RESERVATION_ID, 0), kca_seq_id) IN (
      SELECT
        COALESCE(RESERVATION_ID, 0) AS RESERVATION_ID,
        MAX(kca_seq_id)
      FROM bec_raw_dl_ext.mtl_reservations
      WHERE
        kca_operation <> 'DELETE' AND COALESCE(kca_seq_id, '') <> ''
      GROUP BY
        COALESCE(RESERVATION_ID, 0)
    )
    AND (
      kca_seq_date > (
        SELECT
          (
            executebegints - prune_days
          )
        FROM bec_etl_ctrl.batch_ods_info
        WHERE
          ods_table_name = 'mtl_reservations'
      )
    )
);