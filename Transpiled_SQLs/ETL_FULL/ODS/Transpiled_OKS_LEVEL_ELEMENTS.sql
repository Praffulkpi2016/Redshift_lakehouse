DROP TABLE IF EXISTS silver_bec_ods.OKS_LEVEL_ELEMENTS;
CREATE TABLE IF NOT EXISTS silver_bec_ods.OKS_LEVEL_ELEMENTS (
  ID STRING,
  SEQUENCE_NUMBER STRING,
  DATE_START TIMESTAMP,
  AMOUNT DECIMAL(28, 10),
  DATE_RECEIVABLE_GL TIMESTAMP,
  DATE_REVENUE_RULE_START TIMESTAMP,
  DATE_TRANSACTION TIMESTAMP,
  DATE_DUE TIMESTAMP,
  DATE_PRINT TIMESTAMP,
  DATE_TO_INTERFACE TIMESTAMP,
  DATE_COMPLETED TIMESTAMP,
  OBJECT_VERSION_NUMBER DECIMAL(9, 0),
  RUL_ID STRING,
  CREATED_BY DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  LAST_UPDATED_BY DECIMAL(15, 0),
  LAST_UPDATE_DATE TIMESTAMP,
  SECURITY_GROUP_ID DECIMAL(15, 0),
  CLE_ID STRING,
  DNZ_CHR_ID DECIMAL(15, 0),
  PARENT_CLE_ID STRING,
  DATE_END TIMESTAMP,
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.OKS_LEVEL_ELEMENTS (
  ID,
  SEQUENCE_NUMBER,
  DATE_START,
  AMOUNT,
  DATE_RECEIVABLE_GL,
  DATE_REVENUE_RULE_START,
  DATE_TRANSACTION,
  DATE_DUE,
  DATE_PRINT,
  DATE_TO_INTERFACE,
  DATE_COMPLETED,
  OBJECT_VERSION_NUMBER,
  RUL_ID,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  SECURITY_GROUP_ID,
  CLE_ID,
  DNZ_CHR_ID,
  PARENT_CLE_ID,
  DATE_END,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  ID,
  SEQUENCE_NUMBER,
  DATE_START,
  AMOUNT,
  DATE_RECEIVABLE_GL,
  DATE_REVENUE_RULE_START,
  DATE_TRANSACTION,
  DATE_DUE,
  DATE_PRINT,
  DATE_TO_INTERFACE,
  DATE_COMPLETED,
  OBJECT_VERSION_NUMBER,
  RUL_ID,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  SECURITY_GROUP_ID,
  CLE_ID,
  DNZ_CHR_ID,
  PARENT_CLE_ID,
  DATE_END,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.OKS_LEVEL_ELEMENTS;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'oks_level_elements';