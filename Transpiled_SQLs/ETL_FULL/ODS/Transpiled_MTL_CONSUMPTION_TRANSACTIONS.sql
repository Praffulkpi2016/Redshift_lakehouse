DROP TABLE IF EXISTS silver_bec_ods.mtl_consumption_transactions;
CREATE TABLE IF NOT EXISTS silver_bec_ods.mtl_consumption_transactions (
  TRANSACTION_ID DECIMAL(15, 0),
  CONSUMPTION_RELEASE_ID DECIMAL(15, 0),
  CONSUMPTION_PO_HEADER_ID DECIMAL(15, 0),
  CONSUMPTION_PROCESSED_FLAG STRING,
  REQUEST_ID DECIMAL(15, 0),
  CREATED_BY DECIMAL(15, 0),
  PROGRAM_APPLICATION_ID DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  PROGRAM_ID DECIMAL(15, 0),
  LAST_UPDATED_BY DECIMAL(15, 0),
  PROGRAM_UPDATE_DATE TIMESTAMP,
  LAST_UPDATE_DATE TIMESTAMP,
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  NET_QTY DECIMAL(28, 10),
  BATCH_ID DECIMAL(15, 0),
  RATE DECIMAL(28, 10),
  RATE_TYPE STRING,
  TAX_CODE_ID DECIMAL(15, 0),
  TAX_RATE DECIMAL(28, 10),
  ERROR_CODE STRING,
  RECOVERABLE_TAX DECIMAL(28, 10),
  NON_RECOVERABLE_TAX DECIMAL(28, 10),
  TAX_RECOVERY_RATE DECIMAL(28, 10),
  PARENT_TRANSACTION_ID DECIMAL(15, 0),
  CHARGE_ACCOUNT_ID DECIMAL(15, 0),
  VARIANCE_ACCOUNT_ID DECIMAL(15, 0),
  GLOBAL_AGREEMENT_FLAG STRING,
  NEED_BY_DATE TIMESTAMP,
  ERROR_EXPLANATION STRING,
  SECONDARY_NET_QTY DECIMAL(28, 10),
  BLANKET_PRICE DECIMAL(28, 10),
  PO_DISTRIBUTION_ID DECIMAL(15, 0),
  INTERFACE_DISTRIBUTION_REF STRING,
  TRANSACTION_SOURCE_ID DECIMAL(15, 0),
  INVENTORY_ITEM_ID DECIMAL(15, 0),
  ACCRUAL_ACCOUNT_ID DECIMAL(15, 0),
  ORGANIZATION_ID DECIMAL(15, 0),
  OWNING_ORGANIZATION_ID DECIMAL(15, 0),
  TRANSACTION_DATE TIMESTAMP,
  PO_LINE_ID DECIMAL(15, 0),
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.mtl_consumption_transactions (
  TRANSACTION_ID,
  CONSUMPTION_RELEASE_ID,
  CONSUMPTION_PO_HEADER_ID,
  CONSUMPTION_PROCESSED_FLAG,
  REQUEST_ID,
  CREATED_BY,
  PROGRAM_APPLICATION_ID,
  CREATION_DATE,
  PROGRAM_ID,
  LAST_UPDATED_BY,
  PROGRAM_UPDATE_DATE,
  LAST_UPDATE_DATE,
  LAST_UPDATE_LOGIN,
  NET_QTY,
  BATCH_ID,
  RATE,
  RATE_TYPE,
  TAX_CODE_ID,
  TAX_RATE,
  ERROR_CODE,
  RECOVERABLE_TAX,
  NON_RECOVERABLE_TAX,
  TAX_RECOVERY_RATE,
  PARENT_TRANSACTION_ID,
  CHARGE_ACCOUNT_ID,
  VARIANCE_ACCOUNT_ID,
  GLOBAL_AGREEMENT_FLAG,
  NEED_BY_DATE,
  ERROR_EXPLANATION,
  SECONDARY_NET_QTY,
  BLANKET_PRICE,
  PO_DISTRIBUTION_ID,
  INTERFACE_DISTRIBUTION_REF,
  TRANSACTION_SOURCE_ID,
  INVENTORY_ITEM_ID,
  ACCRUAL_ACCOUNT_ID,
  ORGANIZATION_ID,
  OWNING_ORGANIZATION_ID,
  TRANSACTION_DATE,
  PO_LINE_ID,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  TRANSACTION_ID,
  CONSUMPTION_RELEASE_ID,
  CONSUMPTION_PO_HEADER_ID,
  CONSUMPTION_PROCESSED_FLAG,
  REQUEST_ID,
  CREATED_BY,
  PROGRAM_APPLICATION_ID,
  CREATION_DATE,
  PROGRAM_ID,
  LAST_UPDATED_BY,
  PROGRAM_UPDATE_DATE,
  LAST_UPDATE_DATE,
  LAST_UPDATE_LOGIN,
  NET_QTY,
  BATCH_ID,
  RATE,
  RATE_TYPE,
  TAX_CODE_ID,
  TAX_RATE,
  ERROR_CODE,
  RECOVERABLE_TAX,
  NON_RECOVERABLE_TAX,
  TAX_RECOVERY_RATE,
  PARENT_TRANSACTION_ID,
  CHARGE_ACCOUNT_ID,
  VARIANCE_ACCOUNT_ID,
  GLOBAL_AGREEMENT_FLAG,
  NEED_BY_DATE,
  ERROR_EXPLANATION,
  SECONDARY_NET_QTY,
  BLANKET_PRICE,
  PO_DISTRIBUTION_ID,
  INTERFACE_DISTRIBUTION_REF,
  TRANSACTION_SOURCE_ID,
  INVENTORY_ITEM_ID,
  ACCRUAL_ACCOUNT_ID,
  ORGANIZATION_ID,
  OWNING_ORGANIZATION_ID,
  TRANSACTION_DATE,
  PO_LINE_ID,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.mtl_consumption_transactions;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'mtl_consumption_transactions';