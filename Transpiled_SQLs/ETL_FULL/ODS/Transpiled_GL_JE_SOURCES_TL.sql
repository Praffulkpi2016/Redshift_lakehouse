DROP table IF EXISTS silver_bec_ods.GL_JE_SOURCES_TL;
CREATE TABLE IF NOT EXISTS silver_bec_ods.GL_JE_SOURCES_TL (
  JE_SOURCE_NAME STRING,
  LAST_UPDATE_DATE TIMESTAMP,
  LAST_UPDATED_BY DECIMAL(15, 0),
  OVERRIDE_EDITS_FLAG STRING,
  USER_JE_SOURCE_NAME STRING,
  JOURNAL_REFERENCE_FLAG STRING,
  CREATION_DATE TIMESTAMP,
  CREATED_BY DECIMAL(15, 0),
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  DESCRIPTION STRING,
  ATTRIBUTE1 STRING,
  ATTRIBUTE2 STRING,
  ATTRIBUTE3 STRING,
  ATTRIBUTE4 STRING,
  ATTRIBUTE5 STRING,
  CONTEXT STRING,
  EFFECTIVE_DATE_RULE_CODE STRING,
  JOURNAL_APPROVAL_FLAG STRING,
  LANGUAGE STRING,
  SOURCE_LANG STRING,
  IMPORT_USING_KEY_FLAG STRING,
  JE_SOURCE_KEY STRING,
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.GL_JE_SOURCES_TL (
  JE_SOURCE_NAME,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  OVERRIDE_EDITS_FLAG,
  USER_JE_SOURCE_NAME,
  JOURNAL_REFERENCE_FLAG,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  DESCRIPTION,
  ATTRIBUTE1,
  ATTRIBUTE2,
  ATTRIBUTE3,
  ATTRIBUTE4,
  ATTRIBUTE5,
  CONTEXT,
  EFFECTIVE_DATE_RULE_CODE,
  JOURNAL_APPROVAL_FLAG,
  LANGUAGE,
  SOURCE_LANG,
  IMPORT_USING_KEY_FLAG,
  JE_SOURCE_KEY,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
(
  SELECT
    JE_SOURCE_NAME,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    OVERRIDE_EDITS_FLAG,
    USER_JE_SOURCE_NAME,
    JOURNAL_REFERENCE_FLAG,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    DESCRIPTION,
    ATTRIBUTE1,
    ATTRIBUTE2,
    ATTRIBUTE3,
    ATTRIBUTE4,
    ATTRIBUTE5,
    CONTEXT,
    EFFECTIVE_DATE_RULE_CODE,
    JOURNAL_APPROVAL_FLAG,
    LANGUAGE,
    SOURCE_LANG,
    IMPORT_USING_KEY_FLAG,
    JE_SOURCE_KEY,
    KCA_OPERATION,
    'N' AS IS_DELETED_FLG,
    CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
    kca_seq_date
  FROM bronze_bec_ods_stg.GL_JE_SOURCES_TL
);
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'gl_je_sources_tl';