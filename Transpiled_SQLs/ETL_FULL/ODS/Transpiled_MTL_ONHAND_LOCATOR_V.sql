DROP TABLE IF EXISTS silver_bec_ods.MTL_ONHAND_LOCATOR_V;
CREATE TABLE IF NOT EXISTS silver_bec_ods.MTL_ONHAND_LOCATOR_V (
  organization_id DECIMAL(15, 0),
  inventory_item_id DECIMAL(15, 0),
  padded_concatenated_segments STRING,
  revision STRING,
  total_qoh DECIMAL(28, 10),
  subinventory_code STRING,
  locator_id DECIMAL(15, 0),
  item_description STRING,
  primary_uom_code STRING,
  organization_code STRING,
  organization_name STRING,
  net DECIMAL(15, 0),
  rsv DECIMAL(15, 0),
  atp DECIMAL(15, 0),
  locator_type DECIMAL(15, 0),
  item_lot_control DECIMAL(15, 0),
  item_locator_control DECIMAL(15, 0),
  item_serial_control DECIMAL(15, 0),
  kca_operation STRING,
  is_deleted_flg STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.MTL_ONHAND_LOCATOR_V (
  organization_id,
  inventory_item_id,
  padded_concatenated_segments,
  revision,
  total_qoh,
  subinventory_code,
  locator_id,
  item_description,
  primary_uom_code,
  organization_code,
  organization_name,
  net,
  rsv,
  atp,
  locator_type,
  item_lot_control,
  item_locator_control,
  item_serial_control,
  kca_operation,
  is_deleted_flg,
  kca_seq_id,
  kca_seq_date
)
SELECT
  organization_id,
  inventory_item_id,
  padded_concatenated_segments,
  revision,
  total_qoh,
  subinventory_code,
  locator_id,
  item_description,
  primary_uom_code,
  organization_code,
  organization_name,
  net,
  rsv,
  atp,
  locator_type,
  item_lot_control,
  item_locator_control,
  item_serial_control,
  kca_operation,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.MTL_ONHAND_LOCATOR_V;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'mtl_onhand_locator_v';