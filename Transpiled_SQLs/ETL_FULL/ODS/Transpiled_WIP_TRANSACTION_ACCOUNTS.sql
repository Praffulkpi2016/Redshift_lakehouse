DROP TABLE IF EXISTS silver_bec_ods.WIP_TRANSACTION_ACCOUNTS;
CREATE TABLE IF NOT EXISTS silver_bec_ods.WIP_TRANSACTION_ACCOUNTS (
  TRANSACTION_ID DECIMAL(15, 0),
  REFERENCE_ACCOUNT DECIMAL(15, 0),
  LAST_UPDATE_DATE TIMESTAMP,
  LAST_UPDATED_BY DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  CREATED_BY DECIMAL(15, 0),
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  ORGANIZATION_ID DECIMAL(15, 0),
  TRANSACTION_DATE TIMESTAMP,
  WIP_ENTITY_ID DECIMAL(15, 0),
  REPETITIVE_SCHEDULE_ID DECIMAL(15, 0),
  ACCOUNTING_LINE_TYPE DECIMAL(15, 0),
  TRANSACTION_VALUE DECIMAL(28, 10),
  BASE_TRANSACTION_VALUE DECIMAL(28, 10),
  CONTRA_SET_ID DECIMAL(15, 0),
  PRIMARY_QUANTITY DECIMAL(28, 10),
  RATE_OR_AMOUNT DECIMAL(28, 10),
  BASIS_TYPE DECIMAL(15, 0),
  RESOURCE_ID DECIMAL(15, 0),
  COST_ELEMENT_ID DECIMAL(15, 0),
  ACTIVITY_ID DECIMAL(15, 0),
  CURRENCY_CODE STRING,
  CURRENCY_CONVERSION_DATE TIMESTAMP,
  CURRENCY_CONVERSION_TYPE STRING,
  CURRENCY_CONVERSION_RATE DECIMAL(28, 10),
  OVERHEAD_BASIS_FACTOR DECIMAL(28, 10),
  BASIS_RESOURCE_ID DECIMAL(15, 0),
  GL_BATCH_ID DECIMAL(15, 0),
  REQUEST_ID DECIMAL(15, 0),
  PROGRAM_APPLICATION_ID DECIMAL(15, 0),
  PROGRAM_ID DECIMAL(15, 0),
  PROGRAM_UPDATE_DATE TIMESTAMP,
  GL_SL_LINK_ID DECIMAL(15, 0),
  WIP_SUB_LEDGER_ID DECIMAL(15, 0),
  ENCUMBRANCE_TYPE_ID DECIMAL(15, 0),
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.WIP_TRANSACTION_ACCOUNTS (
  TRANSACTION_ID,
  REFERENCE_ACCOUNT,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  ORGANIZATION_ID,
  TRANSACTION_DATE,
  WIP_ENTITY_ID,
  REPETITIVE_SCHEDULE_ID,
  ACCOUNTING_LINE_TYPE,
  TRANSACTION_VALUE,
  BASE_TRANSACTION_VALUE,
  CONTRA_SET_ID,
  PRIMARY_QUANTITY,
  RATE_OR_AMOUNT,
  BASIS_TYPE,
  RESOURCE_ID,
  COST_ELEMENT_ID,
  ACTIVITY_ID,
  CURRENCY_CODE,
  CURRENCY_CONVERSION_DATE,
  CURRENCY_CONVERSION_TYPE,
  CURRENCY_CONVERSION_RATE,
  OVERHEAD_BASIS_FACTOR,
  BASIS_RESOURCE_ID,
  GL_BATCH_ID,
  REQUEST_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  GL_SL_LINK_ID,
  WIP_SUB_LEDGER_ID,
  ENCUMBRANCE_TYPE_ID,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  TRANSACTION_ID,
  REFERENCE_ACCOUNT,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  ORGANIZATION_ID,
  TRANSACTION_DATE,
  WIP_ENTITY_ID,
  REPETITIVE_SCHEDULE_ID,
  ACCOUNTING_LINE_TYPE,
  TRANSACTION_VALUE,
  BASE_TRANSACTION_VALUE,
  CONTRA_SET_ID,
  PRIMARY_QUANTITY,
  RATE_OR_AMOUNT,
  BASIS_TYPE,
  RESOURCE_ID,
  COST_ELEMENT_ID,
  ACTIVITY_ID,
  CURRENCY_CODE,
  CURRENCY_CONVERSION_DATE,
  CURRENCY_CONVERSION_TYPE,
  CURRENCY_CONVERSION_RATE,
  OVERHEAD_BASIS_FACTOR,
  BASIS_RESOURCE_ID,
  GL_BATCH_ID,
  REQUEST_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  GL_SL_LINK_ID,
  WIP_SUB_LEDGER_ID,
  ENCUMBRANCE_TYPE_ID,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.WIP_TRANSACTION_ACCOUNTS;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'wip_transaction_accounts';