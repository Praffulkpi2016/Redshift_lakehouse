DROP TABLE IF EXISTS silver_bec_ods.OKC_K_ITEMS;
CREATE TABLE IF NOT EXISTS silver_bec_ods.OKC_K_ITEMS (
  ID STRING,
  CLE_ID STRING,
  CHR_ID DECIMAL(15, 0),
  CLE_ID_FOR DECIMAL(15, 0),
  DNZ_CHR_ID DECIMAL(15, 0),
  OBJECT1_ID1 STRING,
  OBJECT1_ID2 STRING,
  JTOT_OBJECT1_CODE STRING,
  UOM_CODE STRING,
  EXCEPTION_YN STRING,
  NUMBER_OF_ITEMS DECIMAL(15, 0),
  PRICED_ITEM_YN STRING,
  OBJECT_VERSION_NUMBER DECIMAL(9, 0),
  CREATED_BY DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  LAST_UPDATED_BY DECIMAL(15, 0),
  LAST_UPDATE_DATE TIMESTAMP,
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  SECURITY_GROUP_ID DECIMAL(15, 0),
  UPG_ORIG_SYSTEM_REF STRING,
  UPG_ORIG_SYSTEM_REF_ID DECIMAL(15, 0),
  PROGRAM_APPLICATION_ID DECIMAL(15, 0),
  PROGRAM_ID DECIMAL(15, 0),
  PROGRAM_UPDATE_DATE TIMESTAMP,
  REQUEST_ID DECIMAL(15, 0),
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.OKC_K_ITEMS (
  ID,
  CLE_ID,
  CHR_ID,
  CLE_ID_FOR,
  DNZ_CHR_ID,
  OBJECT1_ID1,
  OBJECT1_ID2,
  JTOT_OBJECT1_CODE,
  UOM_CODE,
  EXCEPTION_YN,
  NUMBER_OF_ITEMS,
  PRICED_ITEM_YN,
  OBJECT_VERSION_NUMBER,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  LAST_UPDATE_LOGIN,
  SECURITY_GROUP_ID,
  UPG_ORIG_SYSTEM_REF,
  UPG_ORIG_SYSTEM_REF_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  REQUEST_ID,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  ID,
  CLE_ID,
  CHR_ID,
  CLE_ID_FOR,
  DNZ_CHR_ID,
  OBJECT1_ID1,
  OBJECT1_ID2,
  JTOT_OBJECT1_CODE,
  UOM_CODE,
  EXCEPTION_YN,
  NUMBER_OF_ITEMS,
  PRICED_ITEM_YN,
  OBJECT_VERSION_NUMBER,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  LAST_UPDATE_LOGIN,
  SECURITY_GROUP_ID,
  UPG_ORIG_SYSTEM_REF,
  UPG_ORIG_SYSTEM_REF_ID,
  PROGRAM_APPLICATION_ID,
  PROGRAM_ID,
  PROGRAM_UPDATE_DATE,
  REQUEST_ID,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.OKC_K_ITEMS;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'okc_k_items';