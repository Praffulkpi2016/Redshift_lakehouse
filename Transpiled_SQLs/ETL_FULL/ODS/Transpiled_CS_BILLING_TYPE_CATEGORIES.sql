DROP TABLE IF EXISTS silver_bec_ods.CS_BILLING_TYPE_CATEGORIES;
CREATE TABLE IF NOT EXISTS silver_bec_ods.CS_BILLING_TYPE_CATEGORIES (
  BILLING_TYPE STRING,
  BILLING_CATEGORY STRING,
  ROLLUP_ITEM_ID DECIMAL(15, 0),
  START_DATE_ACTIVE TIMESTAMP,
  END_DATE_ACTIVE TIMESTAMP,
  LAST_UPDATE_DATE TIMESTAMP,
  LAST_UPDATED_BY DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  CREATED_BY DECIMAL(15, 0),
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  SECURITY_GROUP_ID DECIMAL(15, 0),
  SEEDED_FLAG STRING,
  ZD_EDITION_NAME STRING,
  ZD_SYNC STRING,
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.CS_BILLING_TYPE_CATEGORIES (
  BILLING_TYPE,
  BILLING_CATEGORY,
  ROLLUP_ITEM_ID,
  START_DATE_ACTIVE,
  END_DATE_ACTIVE,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  SECURITY_GROUP_ID,
  SEEDED_FLAG,
  ZD_EDITION_NAME,
  ZD_SYNC,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  BILLING_TYPE,
  BILLING_CATEGORY,
  ROLLUP_ITEM_ID,
  START_DATE_ACTIVE,
  END_DATE_ACTIVE,
  LAST_UPDATE_DATE,
  LAST_UPDATED_BY,
  CREATION_DATE,
  CREATED_BY,
  LAST_UPDATE_LOGIN,
  SECURITY_GROUP_ID,
  SEEDED_FLAG,
  ZD_EDITION_NAME,
  ZD_SYNC,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.CS_BILLING_TYPE_CATEGORIES;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'cs_billing_type_categories';