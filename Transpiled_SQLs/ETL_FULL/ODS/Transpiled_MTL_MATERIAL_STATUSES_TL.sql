DROP TABLE IF EXISTS silver_bec_ods.MTL_MATERIAL_STATUSES_TL;
CREATE TABLE IF NOT EXISTS silver_bec_ods.MTL_MATERIAL_STATUSES_TL (
  STATUS_ID DECIMAL(15, 0),
  LAST_UPDATED_BY DECIMAL(15, 0),
  LAST_UPDATE_DATE TIMESTAMP,
  CREATED_BY DECIMAL(15, 0),
  CREATION_DATE TIMESTAMP,
  LAST_UPDATE_LOGIN DECIMAL(15, 0),
  STATUS_CODE STRING,
  DESCRIPTION STRING,
  LANGUAGE STRING,
  SOURCE_LANG STRING,
  ZD_EDITION_NAME STRING,
  ZD_SYNC STRING,
  KCA_OPERATION STRING,
  IS_DELETED_FLG STRING,
  kca_seq_id DECIMAL(36, 0),
  kca_seq_date TIMESTAMP
);
INSERT INTO silver_bec_ods.MTL_MATERIAL_STATUSES_TL (
  STATUS_ID,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATE_LOGIN,
  STATUS_CODE,
  DESCRIPTION,
  LANGUAGE,
  SOURCE_LANG,
  ZD_EDITION_NAME,
  ZD_SYNC,
  KCA_OPERATION,
  IS_DELETED_FLG,
  kca_seq_id,
  kca_seq_date
)
SELECT
  STATUS_ID,
  LAST_UPDATED_BY,
  LAST_UPDATE_DATE,
  CREATED_BY,
  CREATION_DATE,
  LAST_UPDATE_LOGIN,
  STATUS_CODE,
  DESCRIPTION,
  LANGUAGE,
  SOURCE_LANG,
  ZD_EDITION_NAME,
  ZD_SYNC,
  KCA_OPERATION,
  'N' AS IS_DELETED_FLG,
  CAST(NULLIF(KCA_SEQ_ID, '') AS DECIMAL(36, 0)) AS KCA_SEQ_ID,
  kca_seq_date
FROM bronze_bec_ods_stg.MTL_MATERIAL_STATUSES_TL;
UPDATE bec_etl_ctrl.batch_ods_info SET load_type = 'I', last_refresh_date = CURRENT_TIMESTAMP()
WHERE
  ods_table_name = 'mtl_material_statuses_tl';